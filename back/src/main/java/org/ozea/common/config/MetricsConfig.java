package org.ozea.common.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MetricsConfig {
    // Spring Legacy에서는 Micrometer 대신 다른 모니터링 방식을 사용
    // 예: AOP를 사용한 커스텀 메트릭 수집
} 