<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- org.ozea.mapper.UserMapper 인터페이스와 매핑되는 MyBatis 매퍼 파일입니다. -->
<mapper namespace="org.ozea.mapper.UserMapper">

    <!-- 이메일을 기준으로 사용자 정보를 조회하는 SQL 쿼리입니다. -->
    <select id="getUserByEmail" parameterType="String" resultType="User">
        SELECT
            user_id,
            name,
            email,
            password,
            mbti,
            phone_num,
            birth_date,
            sex,
            salary,
            pay_amount,
            role
        FROM USER
        WHERE email = #{email}
    </select>

    <!-- 새로운 사용자 정보를 데이터베이스에 삽입하는 SQL 쿼리입니다. -->
    <insert id="insertUser" parameterType="org.ozea.domain.User">
        INSERT INTO USER(user_id,
                         email,
                         name,
                         password,
                         mbti,
                         phone_num,
                         birth_date,
                         sex,
                         salary,
                         pay_amount,
                         role)
        VALUES (#{userId, jdbcType=BINARY, javaType=java.util.UUID},
                #{email, jdbcType=VARCHAR},
                #{name, jdbcType=VARCHAR},
                #{password, jdbcType=VARCHAR},
                #{mbti, jdbcType=VARCHAR},
                #{phoneNum, jdbcType=VARCHAR},
                #{birthDate, jdbcType=DATE},
                #{sex, jdbcType=VARCHAR},
                #{salary, jdbcType=BIGINT},
                #{payAmount, jdbcType=BIGINT},
                #{role, jdbcType=VARCHAR})
    </insert>

    <insert id="insertUserWithEmail" parameterType="org.ozea.domain.User">
        INSERT INTO USER(
            user_id,
            email,
            name,
            password,
            mbti,
            phone_num,
            birth_date,
            sex,
            salary,
            pay_amount,
            role
        ) VALUES (
            #{userId, jdbcType=BINARY, javaType=java.util.UUID},
            #{email, jdbcType=VARCHAR},
            #{name, jdbcType=VARCHAR},
            #{password, jdbcType=VARCHAR},
            #{mbti, jdbcType=VARCHAR},
            #{phoneNum, jdbcType=VARCHAR},
            #{birthDate, jdbcType=DATE},
            #{sex, jdbcType=VARCHAR},
            #{salary, jdbcType=BIGINT},
            #{payAmount, jdbcType=BIGINT},
            #{role, jdbcType=VARCHAR}
        )
    </insert>

    <update id="updateUser" parameterType="org.ozea.domain.User">
        UPDATE USER
        SET name = #{name},
            phone_num = #{phoneNum},
            mbti = #{mbti},
            birth_date = #{birthDate},
            sex = #{sex},
            salary = #{salary},
            pay_amount = #{payAmount},
            role = #{role}
        WHERE email = #{email}
    </update>
</mapper>
